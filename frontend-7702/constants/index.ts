import contractAddress from "./contractAddresses.json";
import abi from "./abi.json";

const contractTypeAddress = contractAddress as { [key: string]: `0x${string}` };

export { contractTypeAddress, abi };
